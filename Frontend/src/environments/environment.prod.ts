export const environment = {
  production: true,
  apiUrl: '/api/v1',          // Uses relative URL — served behind same domain
  wsUrl: '/ws',
  appName: 'SyntaxCore Workforce Platform',
  version: '3.0.0'
};
